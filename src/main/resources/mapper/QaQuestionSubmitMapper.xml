<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fishingtime.qa.mapper.QaQuestionSubmitMapper">

    <insert id="insert" parameterType="com.fishingtime.qa.domain.QaQuestionSubmit"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO multi_qa_question_submit (user_id, category_id, content, description, options_json, status)
        VALUES (#{userId}, #{categoryId}, #{content}, #{description}, #{optionsJson}, #{status})
    </insert>

    <select id="selectById" resultType="com.fishingtime.qa.domain.QaQuestionSubmit">
        SELECT * FROM multi_qa_question_submit WHERE id = #{id}
    </select>

    <select id="selectByStatus" resultType="com.fishingtime.qa.domain.QaQuestionSubmit">
        SELECT * FROM multi_qa_question_submit
        <where>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE multi_qa_question_submit
        SET status = #{status}, reject_reason = #{rejectReason}
        WHERE id = #{id}
    </update>
</mapper>
