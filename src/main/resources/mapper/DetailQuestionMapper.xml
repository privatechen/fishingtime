<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fishingtime.game.mapper.DetailQuestionMapper">

    <select id="selectEnabledByImageKey" resultType="com.fishingtime.game.domain.DetailQuestion">
        SELECT
            id, image_key AS imageKey, question_text AS questionText,
            option_a AS optionA, option_b AS optionB, option_c AS optionC, option_d AS optionD,
            correct_option AS correctOption, difficulty, status,
            created_at AS createdAt, updated_at AS updatedAt
        FROM detail_question
        WHERE image_key = #{imageKey}
          AND status = 1
        ORDER BY id
    </select>

    <select id="selectByImageKey" resultType="com.fishingtime.game.domain.DetailQuestion">
        SELECT
            id, image_key AS imageKey, question_text AS questionText,
            option_a AS optionA, option_b AS optionB, option_c AS optionC, option_d AS optionD,
            correct_option AS correctOption, difficulty, status,
            created_at AS createdAt, updated_at AS updatedAt
        FROM detail_question
        WHERE image_key = #{imageKey}
        ORDER BY id
    </select>

    <select id="selectEnabledImageKeys" resultType="java.lang.String">
        SELECT DISTINCT image_key
        FROM detail_question
        WHERE status = 1
        ORDER BY image_key
    </select>

    <select id="countByImageKey" resultType="long">
        SELECT COUNT(*) FROM detail_question WHERE image_key = #{imageKey}
    </select>

    <delete id="deleteByImageKey">
        DELETE FROM detail_question WHERE image_key = #{imageKey}
    </delete>

    <insert id="insertBatch">
        INSERT INTO detail_question
          (`image_key`, `question_text`, `option_a`, `option_b`, `option_c`, `option_d`, `correct_option`, `difficulty`, `status`)
        VALUES
        <foreach collection="list" item="q" separator=",">
            (#{q.imageKey}, #{q.questionText}, #{q.optionA}, #{q.optionB}, #{q.optionC}, #{q.optionD},
             #{q.correctOption}, #{q.difficulty}, #{q.status})
        </foreach>
    </insert>
</mapper>
