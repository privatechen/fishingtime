<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fishingtime.game.mapper.DetailQuestionMapper">

    <select id="selectEnabledByImageKey" resultType="com.fishingtime.game.domain.DetailQuestion">
        SELECT
            id, image_key AS imageKey, question_text AS questionText,
            option_a AS optionA, option_b AS optionB, option_c AS optionC, option_d AS optionD,
            correct_option AS correctOption, difficulty, status,
            created_at AS createdAt, updated_at AS updatedAt
        FROM detail_question
        WHERE image_key = #{imageKey}
          AND status = 1
        ORDER BY id
    </select>

    <select id="selectEnabledImageKeys" resultType="java.lang.String">
        SELECT DISTINCT image_key
        FROM detail_question
        WHERE status = 1
        ORDER BY image_key
    </select>
</mapper>
