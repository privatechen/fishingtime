<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fishingtime.qa.mapper.QaUserAnswerMapper">

    <select id="selectByUserAndQuestion" resultType="com.fishingtime.qa.domain.QaUserAnswer">
        SELECT * FROM multi_qa_user_answer
        WHERE user_id = #{userId} AND question_id = #{questionId}
    </select>

    <select id="selectByUser" resultType="com.fishingtime.qa.domain.QaUserAnswer">
        SELECT * FROM multi_qa_user_answer
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.fishingtime.qa.domain.QaUserAnswer">
        INSERT INTO multi_qa_user_answer (user_id, question_id, option_id)
        VALUES (#{userId}, #{questionId}, #{optionId})
    </insert>
</mapper>
