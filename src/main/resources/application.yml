server:
  port: 8080

spring:
  application:
    name: fishingtime

  datasource:
    url: jdbc:mysql://123.207.213.29:3306/fishingtime?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: fishingtime
    password: chen199188
    driver-class-name: com.mysql.cj.jdbc.Driver

  thymeleaf:
    cache: false
    suffix: .html
    encoding: UTF-8

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.fishingtime.user.domain,com.fishingtime.banner.domain
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 高德地图配置（Key 仅后端可见）
amap:
  key: b496b1e3d38a1be0502d2f41800aa423
  ip-api: https://restapi.amap.com/v3/ip
  weather-api: https://restapi.amap.com/v3/weather/weatherInfo

# 天气配置
weather:
  default-adcode: 110000
  timeout-ms: 2000

# 微信小程序配置（secret 用环境变量注入，不写死在版本库）
wechat:
  appid: wx13a237243a856ba4
  secret: ${WX_SECRET:}
