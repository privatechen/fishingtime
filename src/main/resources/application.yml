server:
  port: 8080

spring:
  application:
    name: fishingtime

  datasource:
    url: jdbc:mysql://123.207.213.29:3306/fishingtime?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: fishingtime
    password: chen199188
    driver-class-name: com.mysql.cj.jdbc.Driver

  thymeleaf:
    cache: false
    suffix: .html
    encoding: UTF-8

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.fishingtime.user.domain
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
